@layout({ title: 'Log in - Lovefi' })

  <h1>
    {{ t('messages.login') }}
  </h1>
  <form method="POST" action="{{ route('auth.login.store') }}" class="flex flex-col gap-4">
    {{ csrfField() }}
    @!form.input({ type: 'email', name: 'email', displayLabel: t('messages.email') })
    @!form.input({ type: 'password', name: 'password', displayLabel: t('messages.password') })

    @button({ type: "submit" })
      {{ t('messages.login') }}
    @end
    
    @error('E_INVALID_CREDENTIALS')
      <div class="bg-red-300 text-red-800 mt-4 font-weight-bold p-2 rounded">
        <p>
          {{ $message }}
        </p>
      </div>
    @end
  </form>

@end
