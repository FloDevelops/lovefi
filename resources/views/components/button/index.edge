@let(className = 'h-10 px-4 py-2 text-sm font-medium text-zinc-50 bg-zinc-800 rounded-md hover:bg-zinc-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-zinc-700')
@let(nodeName = $props.has('href') ? 'a' : 'button')

<{{nodeName}} {{ $props.merge({ class: [className] }).toAttrs() }}>
  {{{ await $slots.main() }}}
</{{nodeName}}>
